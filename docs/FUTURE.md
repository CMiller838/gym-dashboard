# Parked ideas

## Per-exercise "how to do it" animation

Show a short animation/demo for each exercise during an active workout, so the
user knows the correct form/motion without leaving the app.

**Why parked:** different kind of work from the rest of this batch — needs
per-exercise animation data/assets that wger doesn't provide, so it's either a
licensed content source or hand-built assets, not a quick UI tweak.

**Revisit trigger:** once the rest of this idea batch (prior-set display, sound
feedback, navigation fixes, active-workout persistence bar, elapsed-time fix,
bigger tick button, slideshow, history sort fix, UI refresh) has shipped and
the user still wants form guidance in-app.
